# 코딩 에이전트 작업 가이드라인

## 프로젝트 개요 및 기술 스택

이 프로젝트는 JavaScript를 사용해 동적 웹 사이트를 구현하는 과제입니다.

### 1. JavaScript 작성 금지

- 별도의 허락이나 도움 요청 없이 없다면 JavaScript 파일 작성 및 편집 금지

### 2. HTML, CSS, Vanilla JS만 사용

- HTML, CSS, Vanilla JS(도움 요청시)만 사용해 코드 작성
- 이 프로젝트는 SASS, LESS같은 CSS 전처리기(preprocessor)나 React같은 프레임워크(framework)를 사용하지 않음

## 파일 편집 및 Git 안전 조치

작업 중 발생 가능한 실수를 방지하기 위한 가이드라인입니다.

### 1. 파일 편집 도구 우선 사용

- PowerShell 같은 복잡한 명령어보다 `replace_string_in_file` 같은 안전한 도구를 먼저 사용
- 한두 줄 변경할 때는 도구가 더 안전함

### 2. 작업 전 명확한 확인

- 파일 상태 확인 후, 무엇을 변경할지 먼저 사용자에게 제시
- "이렇게 변경하면 되나요?" 형태로 사전 동의 구하기

### 3. Git 작업 시 백업 의식

- `git checkout -- 파일명` 같은 명령은 파일을 바꾼다는 것을 먼저 사용자에게 알리기
- 복구 필요 시 사용자 명령을 기다리기

### 4. 간단한 것부터 시작

- 복잡한 전략보다는 사용자가 요청한 한 가지 작업에만 집중
- 여러 검증 단계를 넘지 않기

### 5. 명확한 상태 보고

- 너무 많은 터미널 명령을 동시에 실행하지 않기
- 한 번에 한 가지 상태만 확인하고 명확히 보고하기

---

**결론:** 먼저 물어보고, 간단한 도구를 쓰고, 한 번에 한 가지씩 진행하기
